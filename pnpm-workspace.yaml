packages:
  - "apps/*"
  - "packages/*"

# Allowlist for dependency build (postinstall) scripts. esbuild fetches its
# platform-native binary in a postinstall step and is required by Vitest.
allowBuilds:
  esbuild: true
