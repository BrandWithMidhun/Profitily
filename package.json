{
  "name": "profitily-ai",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "infra:up": "docker compose -f tooling/docker-compose.yml --project-directory . up -d --wait && docker compose -f tooling/docker-compose.yml --project-directory . run --rm minio-init",
    "infra:up:ai": "docker compose -f tooling/docker-compose.yml --project-directory . --profile ai up -d --wait && docker compose -f tooling/docker-compose.yml --project-directory . run --rm minio-init",
    "infra:down": "docker compose -f tooling/docker-compose.yml --project-directory . --profile ai --profile init down --remove-orphans",
    "smoke": "node --env-file-if-exists=.env --import tsx tooling/smoke.ts",
    "db:generate": "pnpm --filter @profitily/db generate",
    "db:migrate": "node --env-file-if-exists=.env tooling/run-pkg.mjs @profitily/db migrate:deploy",
    "db:seed": "node --env-file-if-exists=.env --import tsx packages/db/prisma/seed.ts",
    "start:api": "node --env-file-if-exists=.env apps/api/dist/main.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@playwright/test": "^1.61.0",
    "@profitily/config": "workspace:*",
    "@profitily/shared": "workspace:*",
    "@types/pg": "^8.11.10",
    "pg": "^8.13.1",
    "prettier": "^3.5.0",
    "tsx": "^4.19.2",
    "turbo": "^2.5.0"
  }
}
